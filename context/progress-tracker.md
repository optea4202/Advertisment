# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Phase 9: Project Completed

## Current Goal

- None (Project fully implemented, compiled, and ready for deployment).

## Completed

- Optimized render-blocking resources: added `preconnect` headers for Google Fonts to [index.html](file:///x:/Advertisment/client/index.html) and removed the redundant `@import` font rule from [variables.css](file:///x:/Advertisment/client/src/styles/variables.css) to eliminate chained, blocking CSS requests and improve PageSpeed/LCP scores.

- Updated the admin pages management view in `AdminPage.tsx`: removed the search bar and replaced the table view with 2 interactive, square cards for the Home and About pages. Clicking either card directly activates the corresponding page edit form. Unused states and handlers (`pagesSearch`, `handleDeletePage`, and `deletePage` API import) were cleaned up.

- Added a dedicated **Featured Ads** admin tab to the admin dashboard sidebar (`Navbar.tsx`, `AdminPage.tsx`) placed below "Pages". The tab shows the current featured slots (up to 8) as dismissible card rows with numbered badges, and below that a searchable visual grid of all platform ads. Clicking any ad card toggles it in/out of the featured list (cards at the 8-slot limit become disabled). A "Save Changes" button (with spinner state) persists the selection to the home page via the existing `updatePage` API. The tab auto-loads both the full ads list and the current `featured_ad_ids` from the home page on mount.
- Added a text header "Recent Post" to the top of the chronological feed listings on the homepage (`FeedPage.tsx`) below the featured listings.
- Designed and implemented a dynamic featured advertisements spotlight system. Added database migration `019_add_featured_ad_ids_to_pages.sql` adding `featured_ad_ids` array to the `pages` table. Created backend data helpers (`getAdsByIds` in `db/ads.ts`), validation/routing updates, and populated full advertisement objects as `featured_ads` in the home page endpoint (`server/src/controllers/pages.ts`). Updated the admin Page Editor in `AdminPage.tsx` to support configuring up to 8 featured ads. Implemented the responsive glassmorphic featured listings grid on the homepage (`FeedPage.tsx`) positioned cleanly between the Hero Showcase banner and the main listings grid; the spotlight shifts focus through the active page of 4 ads, and once a full cycle of 4 completes, the entire group fades out and is replaced with the next page of featured ads from the pool. Added a pulsing "Featured" overlay badge with a star icon at the bottom-left of the image inside `AdCard.tsx`, automatically highlighting admin-designated featured listings in the home feed, search results, and public user profiles. Rendered a compact Featured overlay badge on the spotlight cards inside the home page's featured ads rectangular box container as well.
- Designed and implemented a dynamic page content management system allowing admins to edit and delete contents for the Home page and About page. Created database migration `018_create_pages.sql` to define the schema and seed default records for slugs `home` and `about`. Added parameterized SQL queries (`db/pages.ts`), service rules (`services/pages.ts`), controllers (`controllers/pages.ts`), and routing definitions (`routes/pages.ts`) on the backend. Created the API wrapper (`api/pages.ts`), added the "Pages" tab to the admin navbar sidebar (`Navbar.tsx`), and built a dynamic edit interface in `AdminPage.tsx` that blocks deleting core system pages or editing their slugs, rendering the form exclusively during edits. Integrated dynamic loaders in `FeedPage.tsx` and `DashboardPage.tsx` with fallback layouts.

- Implemented 500KB image upload size limit across the platform: restricted image uploads to a maximum of 500KB on both frontend and backend. Configured Multer's `limits.fileSize` to `500 * 1024` (500KB) in server routes ([ads.ts](file:///x:/Advertisment/server/src/routes/ads.ts), [users.ts](file:///x:/Advertisment/server/src/routes/users.ts), [chats.ts](file:///x:/Advertisment/server/src/routes/chats.ts)), and updated global error handling middleware in [index.ts](file:///x:/Advertisment/server/src/index.ts) to intercept file size limit errors and respond with a `400 Bad Request` (`FILE_SIZE_LIMIT_EXCEEDED`). Updated React frontend pages ([CreateAdPage.tsx](file:///x:/Advertisment/client/src/pages/CreateAdPage.tsx), [EditAdPage.tsx](file:///x:/Advertisment/client/src/pages/EditAdPage.tsx), [ProfileSetupPage.tsx](file:///x:/Advertisment/client/src/pages/ProfileSetupPage.tsx), [SettingsPage.tsx](file:///x:/Advertisment/client/src/pages/SettingsPage.tsx), [InboxPage.tsx](file:///x:/Advertisment/client/src/pages/InboxPage.tsx)) to validate GIF sizes on selection and compressed image sizes on form submission, showing user-friendly inline or visual validation errors if the limit is exceeded.

- Fixed unauthorized 401 API requests on application mount: updated [WishlistContext.tsx](file:///x:/Advertisment/client/src/context/WishlistContext.tsx) and [ChatContext.tsx](file:///x:/Advertisment/client/src/context/ChatContext.tsx) to check both `user` and `isAuthenticated` before executing API requests for fetching wishlist IDs (`getWishlistIds`) and chat conversations (`getConversations`). This prevents API requests from being fired during early application initialization (before Clerk loaded) or when a stale user profile exists in `localStorage` for unauthenticated guests.

- Fixed the Admin Dashboard Profile Reviews page loading error: reordered route declarations in [user_reviews.ts](file:///x:/Advertisment/server/src/routes/user_reviews.ts) to place the specific admin-only route `/admin/reviews` above the wildcard route `/:id/reviews`. This prevents Express from clashing on the `:id` parameter and intercepting admin requests with a `400 Bad Request` param schema parsing failure.

- Fixed PWA offline mode: enabled service worker in dev mode (`devOptions: { enabled: true }`), fixed the API cache URL pattern from a broken regex to a function-based matcher (`url.pathname.startsWith('/api/')`), added Cloudinary image caching (CacheFirst, 30 days), added `navigateFallback: '/offline.html'` so users see a branded offline page when completely offline with no cache, created `public/offline.html` with auto-reconnect logic, created the missing `InstallBanner.tsx` component (handles Android Chrome native install prompt and iOS "Add to Home Screen" instructions, persists dismissal in localStorage), integrated `InstallBanner` into `App.tsx`, and updated `main.tsx` to explicitly register the service worker via `registerSW` with a non-blocking "Update available" toast notification. Added `vite-plugin-pwa/client` types to `tsconfig.app.json` to prevent TypeScript errors on the virtual module import.

- Designed and implemented the Profile Ratings & Reviews system allowing users to rate and review other users' public profiles. Executed database migration `017_create_user_reviews.sql` creating a `user_reviews` table with self-review check constraints and a unique pair constraint for reviewer and target. Built backend CRUD functions, services, controllers, and mounted routes at `/api/users/:id/reviews` (for creation and listings) and `/api/users/reviews/:reviewId` (for deletion). Updated frontend profile types, created API request wrapper (`userReviews.ts`), state management custom hook (`useUserReviews.ts`), and user-facing forms/lists components (`ProfileReviewForm` and `ProfileReviewList`). Integrated review tab, statistics (avg rating, count), and submission form into the public user profile page (`UserProfilePage.tsx`). Configured search/moderation tab in the admin panel (`AdminPage.tsx` and `Navbar.tsx`) enabling administrators to audit, search, and delete profile reviews.
- Replaced the Star Rating & Written Reviews feature with a text-only Comments system. Created database migration `016_change_reviews_to_comments.sql` to back up existing null/empty review text based on their star rating, drop the `star_rating` column and check constraint, and make `review_text` required. Cleaned up backend schemas, controllers, database queries, and email utilities. Updated frontend components (`ReviewForm` to `CommentForm`, `ReviewList` to `CommentList`, `ReportModal`, `Navbar`, and stats page calculations) to remove rating stars and represent all feedback as public text comments.
- Configured the navigation bar's search input (`SearchBar`) and category filters (`CategoryFilter`) to render only when the user is on the Home page (`/`) or Search page (`/search`), removing them from all other pages like Inbox (`/inbox`), Settings (`/settings`), user profiles (`/profile/:id`), Create/Edit Ad, and Ad Detail.
- Implemented a premium interactive full-screen Lightbox Modal for advertisement detail images (in `ImageGallery.tsx`) enabling users to zoom and pan. Features include zoom-in/zoom-out buttons (+/-), scale percentage indicator, size presets (Small at 100%, Medium at 250%, Large at 400%), resetting, panning/dragging coordinates for mouse/touch inputs, scroll wheel zoom, double-click/double-tap to toggle zoom, keyboard navigation (Escape, Left/Right arrows), body scroll lock when open, and visual indicators.
- Repositioned the Category Bar for mobile viewports: moved it to the very top of the page body contents on both the homepage (`FeedPage.tsx`) and search page (`SearchPage.tsx`), positioning it directly underneath the mobile search bar (which is at the bottom of the sticky header). Kept the desktop Category Bar layout inside the navigation header unchanged.
- Positioned the search bar at the bottom of the sticky navigation header for mobile viewports to align with the design mockup. Handled state updates via URL Search parameters and routed queries properly. Removed the duplicated inline page-level search inputs from `FeedPage.tsx` and `SearchPage.tsx` on mobile devices.
- Enabled category filter bar and search bar keyword searches to work together by configuring Algolia index settings (`attributesForFaceting` to include `categories` and `category`) and modifying client-side home feed filters to preserve parameters. Unified peer-to-peer chat logs to link all chats between the same user pair into a single shared conversation rather than separate chats per ad. Introduced visual, clickable ad inquiry markers in chat threads (showing ad title and link to details) when contacting a seller from an advertisement detail page.
- Created a dedicated search results page (`SearchPage.tsx` at `/search`) to display search queries and category filter results. Simplified the main landing page (`FeedPage.tsx` at `/`) to show the spotlight ad, categories, and the general latest advertisements feed. Updated search and category selection handlers (both on desktop Navbar and mobile FeedPage inline inputs) to automatically navigate the user to `/search` with URL parameters. Configured checks to prevent empty search/category queries from triggering redirections from the home page, and added a redirect on the search page to send the user back to the homepage `/` once all filters are cleared.
- Fixed a CSS clipping issue where the SearchBar dropdown suggestions and search history panels were rendered into the DOM but hidden on desktop because their parent container had `overflow-x-auto`. Added the `md:overflow-visible` utility class to that container in `client/src/components/Navbar.tsx` so the dropdown menu overflows the navigation bar correctly, restoring the visibility and clickability of search suggestions and search history.
- Restored direct Algolia client-side search suggestions in `SearchBar.tsx`: initialised the Algolia v5 search client (using `VITE_ALGOLIA_APP_ID` + `VITE_ALGOLIA_SEARCH_ONLY_API_KEY`, mirroring the pattern in `useAds.ts`); updated the suggestions state to carry a `fromAlgolia` boolean; in the fetch effect, ran `getSearchSuggestions` (for category names) and `searchClient.searchSingleIndex` (for ad title hits) concurrently with `Promise.allSettled`, so a single Algolia failure never blocks category results; when Algolia returns title hits, they are labelled with a ⚡ Algolia badge in the dropdown; falls back silently to backend DB titles when the Algolia client is unavailable or returns no results; search history (recent searches, per-item delete, Clear All) is fully preserved.
- Replaced the "All Categories" menu button in the navigation category bar with hover-activated category buttons on desktop. When a parent category is hovered, a Gameloot-style compact vertical dropdown box (`w-[220px]`) pops down. If a subcategory in that dropdown has Level 3 items, hovering over it reveals a secondary fly-out box showing its sub-items. We added height constraints (`max-h-[350px]`) and vertical scrollbars (`overflow-y-auto`) to both dropdown boxes so lists with large quantities of categories are scrollable and fully accessible. The layout automatically right-aligns for buttons near the row end to prevent off-screen overflow, and closes naturally upon category selection.
- Integrated search filters into the navigation header: embedded `SearchBar` next to the "Fakna" brand logo (expanded/resized up to `800px` max-width) in the top row of the sticky `Navbar.tsx` and mounted an OLX-style `CategoryFilter` in a new second row at the bottom of the navigation bar for desktop viewports. The OLX-style layout features an "All Categories" toggle button that opens a structured 2-pane mega menu (left panel showing primary categories with inline subcategory expansion, and a right 3-column grid showing sub-items). Removed the "All" options from the filter layouts (both desktop mega-menu and mobile chip layouts) and enabled selection toggling on active category click. Synced search query, category, and page state globally via URL Search Parameters (`useSearchParams`) to support search/navigation flows from other pages. Configured `FeedPage.tsx` to hide search inputs on desktop while keeping them visible on mobile viewports.
- Modified `client/src/components/Navbar.tsx` to completely remove the Home and My Profile links (both text and icons/logos) from the navigation bar, keeping standard navigation links exclusively for admin sidebars. Repositioned the About link to the right-aligned actions area next to the Sign Out button (for logged-in users) and Sign In button (for guest users) as an icon-only button, deleting the redundant mobile About button logic.
- Created local environment configuration files ([server/.env](file:///x:/Advertisment/server/.env) and [client/.env](file:///x:/Advertisment/client/.env)) with placeholder variables for Clerk, Cloudinary, Resend, Algolia, and PostgreSQL to simplify development setup and local application launch.
- Implemented search result metadata display on the feed page: added a results info banner above the advertisement grid in `client/src/pages/FeedPage.tsx` showing the current page range and total matching result count for the active search query (e.g. "1-12 of 68 results for \"query\""), styled to match the design system with premium typography and animated entrance transitions.
- Implemented marketplace feed pagination with a size of 12: added support for `page` and `limit` filters in database querying `getAds` and introduced `getAdsCount` to return total matching ads in `server/src/db/ads.ts`; expanded the Zod query schema and `handleGetAds` controller in `server/src/controllers/ads.ts` to validate inputs and response with structured paginated metadata; updated client-side API wrappers in `client/src/api/ads.ts`; adjusted `useFeed` custom hook in `client/src/hooks/useAds.ts` to handle paginated cache, configure Algolia keyword search pagination parameters, and return pagination totals; and added local page state, reset observers, and a premium responsive page selector component with next/prev navigation to `client/src/pages/FeedPage.tsx`.
- Fixed server deployment crash: modified Zod configuration validation in `server/src/config/index.ts` to make `ALGOLIA_APP_ID` and `ALGOLIA_ADMIN_API_KEY` optional, and updated `server/src/utils/algolia.ts` to safely initialize the `algoliasearch` client conditionally and add null checks in sync functions. This fully prevents any `appId` is missing or `TypeError` crashes on startup or runtime, allowing the server to boot and search safely, falling back to database suggestions when Algolia is disabled.
- Implemented Client-Side Search History in the Search Bar: added support for saving the last 5 search queries in `localStorage` (`fakna_search_history`); updated `SearchBar.tsx` to load search history on mount and save queries when pressing "Enter" or selecting an ad title suggestion; created `saveToHistory`, `deleteHistoryItem`, and `clearAllHistory` handlers; modified `handleFocus` to display the "Recent Searches" dropdown when the search input is focused and empty; rendered a list of recent searches styled with a history icon, individual item deletion buttons, and a "Clear All" action button; and optimized dropdown visibility to only render when there is active content to show.
- Implemented autocomplete search suggestions dropdown on the home feed: added `getDbSuggestions` database query function to `server/src/db/ads.ts` that fetches matching category names and unique ad titles via `ILIKE`; added `handleGetSuggestions` controller to `server/src/controllers/ads.ts` that returns category matches from PostgreSQL and title matches from Algolia (with DB fallback); registered `GET /api/ads/suggestions?q=` route in `server/src/routes/ads.ts` before the `/:id` wildcard; added `getSearchSuggestions` API wrapper in `client/src/api/ads.ts`; rewrote `SearchBar.tsx` with a 200ms debounced suggestion fetcher, a glassmorphic dropdown showing categorized "Categories" (with folder icon) and "Advertisements" (with sell icon) suggestions, click-outside and Escape-to-close handlers, and a live loading spinner; extended `SearchBarProps` with an `onSelectCategory` callback; and passed `setCategory` from `FeedPage.tsx` to `SearchBar` so clicking a category suggestion applies the category filter directly.
- Fixed a top-level initialization crash in `client/src/pages/InboxPage.tsx` caused by instantiating the Algolia v5 client directly with placeholder values. Initialized the client inside a try-catch block and added checks for placeholders. Implemented a fallback database search (via the existing `searchUsers` API wrapper) for user searches within the inbox page when Algolia is unavailable, fully preventing the login white-screen crash.
- Configured and initialized Algolia v5 search client on both the backend and frontend: added environment validation schemas for Algolia config variables in `server/src/config/index.ts`, created the backend indexing handlers in `server/src/utils/algolia.ts` and the backfill script in `server/src/scripts/backfill-algolia.ts`, added `"backfill:algolia"` command to `server/package.json`, and implemented Strategy B (Direct Client-Side Search) by modifying the search queries in `client/src/hooks/useAds.ts` (for the home advertisement feed) and `client/src/pages/InboxPage.tsx` (for user chat searches) to call Algolia v5 directly from the browser.
- Designed and implemented a responsive, multi-column universal `Footer` component aligned with the visual identity and styling tokens of the design system. The footer features a grid layout (collapsing cleanly on mobile viewports), rich animations on hover for links and social media buttons, support for system-wide light/dark themes, and a dynamic systems health status badge. Integrated the component across all public and user-facing views: `FeedPage.tsx`, `AdDetailPage.tsx`, `UserProfilePage.tsx`, `CreateAdPage.tsx`, `EditAdPage.tsx`, `SettingsPage.tsx`, and replaced the inline custom footer in `DashboardPage.tsx` with the new universal component.
- Redesigned the category selection interface on the home feed (`FeedPage.tsx`) to support a multi-tier tree structure on both PC and mobile viewports: created database migration `015_seed_pc_category_tree.sql` to seed the exact `PC` -> `PC Components` -> `PC Graphics Card`, etc. hierarchy; rewrote the `CategoryFilter.tsx` component into a horizontal mega-menu bar for desktop, featuring delay-mitigated hover menus that display Level 2 subcategories on the left and Level 3 on the right; implemented a responsive mobile-friendly sliding bottom sheet drawer in `CategoryFilter.tsx` utilizing a side-by-side split layout to enable deep category selection with clear breadcrumb routing; modified the main page layout in `FeedPage.tsx` to mount the new category bar horizontally under the search row, replace the vertical filter sidebar with a dismissible active filters summary banner, and expand the advertisement cards grid to a full-width, 4-column layout on desktop views.
- Implemented hierarchical category sub-trees for both users and admin: added database migration `014_add_parent_id_to_categories.sql` introducing a `parent_id` foreign key column (referencing `categories(id) ON DELETE SET NULL`) and seeding default subcategories (e.g., "Laptops" and "Phones" under "Electronics", "Chairs" under "Furniture", and "Cars" under "Vehicles"); updated backend query files, services, Zod schemas, and request controllers to handle `parent_id` with strict cyclic loop check validations; updated backend ads searching queries with recursive CTE fetches so selecting a parent category retrieves all ads in its child categories; created category tree formatting utilities (`categoryTree.ts`); modified the ad creation and editing forms to display categories with nesting indent levels and direction glyphs (e.g., `  ↳ Laptops`); updated the Home feed filter sidebar with a collapsible category tree with expanding/collapsing chevrons for desktop and a secondary row of horizontal sub-chips for mobile; and updated the admin categories moderation tab to include parent selector dropdown fields and display the category list table as a visual tree.
- Added category symbol selection for admin moderation: added migration `013_add_symbol_to_categories.sql` introducing a `symbol` column to the `categories` table (seeded with 'devices', 'chair', etc.); modified database functions, service validations, Zod parser schema, and controllers on the backend to manage the `symbol` property; updated the client-side API wrappers; updated `CategoryFilter.tsx` to dynamically fetch and map category names to database-stored symbols instead of relying on a hardcoded map; and added a premium inline Material Symbol selector grid dropdown placed to the left of the category name input (for both create and edit modes) inside the admin `AdminPage.tsx` tab panel, resizing it to look compact and visually polished.
- Implemented dynamic category controller for ads: added database migration `012_create_categories.sql` introducing a `categories` table with seeded defaults (`'Electronics'`, `'Furniture'`, `'Vehicles'`, `'Services'`, `'Other'`); added backend query files, services, controllers, Zod validators, and mounted REST API under `/api/categories`; created the Category tab layout in the admin Moderation Dashboard (`AdminPage.tsx`) allowing full CRUD operations; integrated cascading category name updates (`ON UPDATE CASCADE`) and fallback mappings (`ON DELETE SET DEFAULT` mapping to `'Other'`); updated `Navbar.tsx` to mount "Categories" below "Reports" in the admin sidebar; dynamically updated frontend category consumers (`CategoryFilter.tsx`, `CreateAdPage.tsx`, `EditAdPage.tsx`, `AdminHomePage.tsx`) to pull lists directly from the database; and built a custom premium React-based `ConfirmModal` component to replace browser-native confirmation alert dialogs for deletion/moderation events in the admin moderation dashboard.
- Corrected the left vertical sidebar to be strictly admin-only: all sidebar/column layout classes (fixed position, full height, 240px width, flex-col, padding offsets) in `Navbar.tsx` are now conditional on `user?.is_admin`. Regular users and guests now see the original horizontal sticky top bar with icons-only compact controls, no column layout leakage. The admin profile mock added for testing was also reverted in `AuthContext.tsx`. Pushed as commit `d405865`.
- Implemented the Content Reporting System: added a database migration `011_create_reports.sql` for the `reports` table; implemented backend database queries, business validations (entity existence checks and self-reporting blocks), controllers, routes, and mounted under `/api/reports`; created the frontend `ReportModal` component with interactive predefined options; integrated report action button overlays on `AdDetailPage` (ads and review list items) and `UserProfilePage`; and added the "Reports" tab in the `AdminPage` moderation panel enabling administrators to resolve reports or moderate targets directly with cascading database deletions.
- Changed website text/buttons from "Feed" to "Home" across the client application (Navbar, AdDetailPage, UserProfilePage, DashboardPage). Added a "Back to Home" button at the top-left (left top) of the login and signup pages for both desktop and mobile viewports.
- Enabled guest browsing: removed the mandatory authentication gateway from the main feed page, search/filters, about page, ad detail page, and public profile page. Introduced backend optional authentication middleware. Restricted mutative and private actions (posting ads, inbox chat, writing reviews, and wishlists) to registered users with direct login redirects.
- Replaced Leaflet.js interactive maps on `CreateAdPage` and `EditAdPage` with an iframe-rendered interactive map picker (`map-picker.html`), which listens for click/pin events via cross-window communication (`postMessage`). This resolves React Leaflet bundle bloat and rendering/sizing issues inside tabs/flex grids while keeping coordinate and address auto-filling intact.
- Changed the price input field currency symbol prefix from dollars ($) to Indian Rupees (₹) in both the Create and Edit ad pages to match the rest of the application's currency display.
- Client project foundation initialized with React, Vite, TS, Tailwind CSS, and custom design tokens in `variables.css`.
- Server project foundation initialized with Express, TS, Cors, tsx, and Zod env validator.
- Database migrations 001 through 004 written and successfully applied to Neon PostgreSQL.
- Express entrypoint with database-connected health check `/health` returning `200 OK`.
- Clerk Authentication integrated on both backend (JWT token verification middleware + ban status checks) and frontend (React Routing, ClerkProvider, AuthProvider, and ProtectedRoute).
- Profile Setup Page completed with frontend input validations and Cloudinary profile photo upload.
- Ad posting bento form and personal inventory list page fully implemented, supporting multi-image upload up to 5 files using Cloudinary.
- Ad Editing Page and deletion actions fully integrated (handles Cloudinary deletion cleanup for removed/swapped images and database references).
- Ad Feed page, SearchBar with debouncing, CategoryFilter, and custom hooks fully implemented (Unit 5).
- Ad Detail Page, responsive navigable ImageGallery carousel, useAd hook, and routes fully implemented (Unit 6).
- Star rating submissions, self-review checks, Resend utility helper, review endpoints, and ReviewForm/ReviewList components fully integrated (Unit 7).
- Admin panel, tabbed sections (Ads, Reviews, Users), admin ban account triggers, admin deletion actions, and AdminRoute guard fully integrated (Unit 8).
- Production build commands, environment variables schema validation, and deployment plan finalized (Unit 9).
- Verified that both the server and client builds compile successfully.
- Resolved token expiration/refresh bug: replaced static setAuthToken with dynamic Axios request interceptors that retrieve and attach fresh Clerk JWT tokens before every API request.
- Implemented system-wide Dark Mode: added theme variables in `variables.css`, created `ThemeContext`, wrapped the application, and added a custom toggle button in the `Navbar`.
- Implemented Profile Settings: created a dedicated `/settings` route/page allowing users to edit profile username, phone number, bio, and photo (with Cloudinary uploads), integrating form validation and error/success alerts.
- Implemented Public User Profiles: created `GET /api/users/:id` backend endpoint returning a safe public profile (no email/phone/clerk_id), added `UserProfilePage` at `/profile/:id`, added "My Profile" nav link, made the avatar avatar and publisher cards on ad detail page clickable links to user profiles. Other users can browse any non-banned user's profile and their ads.
- Implemented a mobile-friendly Floating Action Button (FAB) for posting advertisements on small viewports (`sm:hidden`), with interactive hover/click micro-interactions and custom entering bounce animation (`bounce-short`), ensuring visibility is gated to non-creation/edit routes.
- Optimized page navigation and initial loading times by establishing database indexes on relational foreign keys (`ads.owner_id`, `ad_images.ad_id`, `reviews.ad_id`, `reviews.reviewer_id`), implementing client-side Stale-While-Revalidate (SWR) caching for ads, profiles, and feeds, caching database user profiles in `localStorage` in `AuthContext` to skip session loading screen wait times, and registering a centralized cache invalidation response interceptor on all write requests (`POST`, `PUT`, `DELETE`).
- Added a mobile-only About (info) icon button to the header to enable access on portrait viewports, and added a footer credit saying "Developed by optea" to the About page.
- Enabled users to edit and delete their advertisement posts directly from their own user profile page by conditionally passing action triggers to `AdCard` components when viewing their own profile.
- Implemented peer-to-peer Chat with Seller feature: added `conversations` and `messages` PostgreSQL tables (migration 006), created full backend REST API (`/api/chats`) with Zod validation, auth guards, and participant ownership checks, built `InboxPage` with responsive split-screen desktop / toggled mobile layout, SWR-style polling at 3.5s intervals, message bubbles, and auto-scroll. Added an inbox icon button to the Navbar (left of theme toggle on desktop, floating FAB above the post button on mobile), and a "Chat with Seller" CTA button on `AdDetailPage` (hidden for ad owners).
- Fixed mobile inbox styling to use dynamic viewport height (100dvh) for a full-screen, locked, double-scrollbar-free layout, and configured mobile floating action buttons (FABs) to hide specifically when viewing the `/inbox` route to avoid visual clutter and overlay conflicts.
- Stabilized mobile chat window layout against keyboard zoom/scrolling shifts using the VisualViewport API, resetting window scroll, and caching/passing the seller profile metadata across routers (`location.state`) to eliminate header auto-hiding during initial load and polling updates.
- Implemented unread message notifications by introducing a shared ChatContext that polls conversations, tracks read states in localStorage, and renders a pulsing emerald dot on the Navbar inbox icon. Removed the duplicate floating Inbox FAB on mobile view for a cleaner layout.
- Added "Message" button to other users' public profile pages so users can start a direct chat without needing to view an ad. Added photo upload support in the chat input area: users can attach an image (with a live thumbnail preview), send image-only or image+text messages via Cloudinary, and images render as tappable/zoomable bubbles in the chat thread. Required DB migration 007 to add `image_url` column to the `messages` table.
- Designed and implemented a responsive, fancy, and eye-catching landing page (`FeedPage`) for both PC and mobile viewports, including a premium Hero Showcase with animated text gradients, animated gradient flow background, glassmorphism spotlight preview card featuring automatic sliding rotation and navigation dots, active listing statistics, and visual category icons.
- Made the entire advertisement card container a semantic Link component (with cursor-pointer) on both the primary AdCard and the spotlight preview card to ensure native browser/touch navigation support (including on mobile portrait views). Nested action buttons (Edit, Delete, and Slider dots) have event propagation and default actions blocked to keep their interactions separate and fully functional. Removed the "Open Ad" button.
- Added client-side image compression using HTML5 Canvas (`client/src/utils/imageCompressor.ts`) in the ad creation/editing forms and profile setup/settings. This reduces large high-resolution camera images from up to 10MB down to a few hundred kilobytes (and small 400px profile avatars) on the fly, speeding up ad publishing and updates on mobile networks by up to 95%.
- Implemented PWA (Progressive Web App) support to enable iOS users to install AdHub directly on their iPhone/iPad Home Screen. Changes: added `manifest.webmanifest` with brand teal theme, added iOS-specific meta tags (`apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`, `apple-touch-icon`) in `index.html`, configured `vite-plugin-pwa` with Workbox (network-first API caching, cache-first Google Fonts), added `--safe-area-inset-bottom` CSS variable for iPhone home indicator clearance, and created `InstallBanner` component that detects iOS Safari non-standalone mode and guides users through the Add to Home Screen flow.
- Implemented message edit, message delete, and conversation delete for P2P chat. DB migration 008 adds `is_edited BOOLEAN DEFAULT FALSE` to `messages`. New backend endpoints: `PUT /api/chats/messages/:messageId` (edit own message text, server-side ownership check), `DELETE /api/chats/messages/:messageId` (delete own message with Cloudinary cleanup if image), `DELETE /api/chats/:id` (delete entire conversation with Cloudinary cleanup of all message images). Frontend: `useMessages` hook exposes `editMsg` and `deleteMsg` with optimistic local state; InboxPage shows hover-reveal edit/delete buttons on own messages, inline edit textarea with Save/Cancel (Enter to save, Escape to cancel), inline delete confirmation ("Delete? Yes / No"), `(edited)` label after timestamp on edited messages, and a trash icon in the chat header that opens a confirmation modal before deleting the whole conversation.
- Implemented user search bar inside the Inbox left panel: added `GET /api/users/search?q=` backend endpoint (ILIKE query, excludes banned users and self, capped at 10 results) with DB function, service wrapper, controller, and route (registered before `/:id` to avoid Express parameter clash); added `searchUsers` client API wrapper; added debounced (300ms) search input with animated dropdown inside the InboxPage left panel — shows avatar, username, bio, and a send icon per result, clicking any result calls `startConversation` and opens the chat thread immediately.
- Changed the website name and branding from AdHub to Fakna across all pages, components, SEO meta tags, email templates, configuration files, and package metadata.
- Implemented the Wishlist feature: added database table `wishlist` (migration 009) with CASCADE triggers, built backend endpoints (`/api/wishlist` and `/api/wishlist/ids`), created a global frontend `WishlistProvider` for optimistic love symbol button updates, integrated the heart button overlays on `AdCard` and `AdDetailPage`, and added a private, reactive `My Wishlist` tab on the user's own profile page.
- Implemented Google Maps style location picker: integrated Leaflet.js + OpenStreetMap interactive map on `CreateAdPage` and `EditAdPage` (click-to-pin, dynamic map centering, Nominatim reverse geocoding auto-fills address field). Added latitude/longitude columns to the `ads` table (migration 010) and extended the DB, Service, Controller, and API endpoints. Added free Google Maps iframe embed on `AdDetailPage` to display the pinned location when coordinates exist.
- Added a Share button to `AdDetailPage` next to the Wishlist button. On mobile/supported browsers it invokes the native Web Share sheet (`navigator.share`); on desktop it copies the current page URL to the clipboard and shows a 2-second "Link copied!" pill toast with a fade-in-up animation.
- Added a share button overlay to the `AdCard` component underneath the wishlist favorite heart icon, which reveals a premium sharing overlay covering the image container with social media sharing targets (WhatsApp, Telegram, Facebook, Twitter / X) and a copy link option.
- Added client-side search inputs to the Admin Moderation Dashboard: "Search by title" on the Ads tab, "Search by comment" on the Reviews tab, and "Search by username" on the Users tab. Each input has a search icon prefix, a clear (×) button, and shows a contextual empty-state message when no matches are found.
- Implemented a complete dashboard for the Admin Home page (`AdminHomePage.tsx` / `/admin/home`), rendering visual metrics cards (Active Ads, Total Users, Active vs Banned counts, Reviews, Average Rating, Pending Reports), an interactive custom CSS/HTML category distribution chart, quick actions, a feed of recent reports needing action, and recently posted advertisements.

- Implemented and verified SEO Open Graph link previews: created `server/src/routes/og.ts` — a new Express endpoint at `/og/ads/:id` that fetches an ad from PostgreSQL via the existing `getAdById` DB function and returns a minimal HTML page pre-populated with `og:title`, `og:description`, `og:image`, and `twitter:card` meta tags (with XSS escaping and a 10-minute `Cache-Control`). Mounted the route in `server/src/index.ts`. Added `FRONTEND_URL` as an optional env var to `server/src/config/index.ts`. Updated `client/vercel.json` with a bot-conditional rewrite rule: when any known social crawler (WhatsApp, Facebook, Discord, Twitter, LinkedIn, etc.) hits an `/ads/:id` URL, Vercel proxies the request to the OG endpoint on Render; real browsers continue to receive the React SPA. Added default Open Graph and Twitter Card `<meta>` tags to `client/index.html` for the homepage. Generated a 1200×630 branded default OG image and placed it in `client/public/og-default.png`. Created `client/public/robots.txt` allowing all crawlers with private routes (`/admin`, `/dashboard`, `/settings`, `/inbox`, `/profile-setup`) disallowed. Verified the deployment live by curling with simulated bot User-Agents (e.g. WhatsApp crawler) and regular browser clients, validating proper header redirects and dynamic tag mapping.


## In Progress

- None.

## Next Up

- None.

## Open Questions

- None.

## Architecture Decisions

- Configured TailwindCSS v3 mapped to design system CSS variables in `variables.css`.

## Session Notes

- FeedPage, SearchBar, CategoryFilter, and useFeed hooks completed (Unit 5).
- AdDetailPage, ImageGallery (navigable carousel with thumbnail list), and useAd hooks completed (Unit 6).
- ReviewForm, ReviewList, useReviews hook, backend services/routes, and Resend email alerts completed (Unit 7).
- AdminPage tabs, admin API callers, requireAdmin server middleware, and AdminRoute component route guards completed (Unit 8).
- Finalized production configs, verified zero tsc errors, and generated release walkthrough logs (Unit 9).
- Verified search query ILIKE parsing and category database filters on the backend.
- Excluded banned users' ads from the public feed.
- Verified the Featured Ads admin control tab UI, slots management, search picker, and save persistence, and confirmed the homepage Featured Listings section renders and cycles the selected ads correctly.